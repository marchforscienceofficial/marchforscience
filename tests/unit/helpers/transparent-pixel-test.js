
import { transparentPixel } from 'marchforscience/helpers/transparent-pixel';
import { module, test } from 'qunit';

module('Unit | Helper | transparent pixel');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = transparentPixel([42]);
  assert.ok(result);
});

